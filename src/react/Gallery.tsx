"use client";

import { useMemo, useState } from "react";
import Link from "next/link";
import { ArrowLeft, CheckCircle2, Layers3, Search, ShieldCheck } from "lucide-react";
import { demoScenarios, widgetFixtures, withDemoState } from "@/src/fixtures";
import { parseRenderEnvelope } from "@/src/core";
import type { KnownRenderEnvelope } from "@/src/schema";
import { RendererHost } from "./RendererHost";
import { rendererRegistry } from "./renderer-registry";

export function Gallery() {
  const [search, setSearch] = useState(""); const [group, setGroup] = useState("All"); const [widgetState, setWidgetState] = useState<"ready" | "loading" | "empty" | "error">("ready");
  const manifest = rendererRegistry.manifest().filter((item) => item.type !== "*"); const groups = ["All", ...new Set(manifest.map((item) => item.type.split(".")[0]))]; const filtered = manifest.filter((item) => (group === "All" || item.type.startsWith(`${group}.`)) && (!search || `${item.displayName} ${item.type}`.toLowerCase().includes(search.toLowerCase())));
  const widgets = useMemo(() => widgetFixtures.map((item) => widgetState === "ready" ? item : withDemoState(item, widgetState)), [widgetState]);
  return <main className="docs-shell"><header className="docs-topbar"><Link href="/" className="button secondary"><ArrowLeft /> Playground</Link><div className="brand-link"><span className="logo-mark"><i /><i /><i /></span><span><strong>Component Gallery</strong><small>Renderers, widgets and states</small></span></div><Link href="/inspector" className="button secondary"><ShieldCheck /> Inspector</Link></header><section className="docs-hero"><p className="eyebrow">Capability catalog</p><h1>Every renderer has a contract.</h1><p>Search the registry manifest, inspect supported formats, and exercise controlled widgets across loading, empty, error and ready states.</p><div className="gallery-stats"><div><strong>{manifest.length}</strong><span>registered renderers</span></div><div><strong>{widgetFixtures.length}</strong><span>business widgets</span></div><div><strong>3</strong><span>trust paths</span></div></div></section><section className="gallery-section"><div className="gallery-heading"><div><p className="eyebrow">Renderer registry</p><h2>Capabilities</h2></div><label className="sidebar-search"><Search /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search renderers" /></label></div><div className="filter-row">{groups.map((item) => <button type="button" className={group === item ? "active" : ""} key={item} onClick={() => setGroup(item)}>{item}</button>)}</div><div className="capability-grid">{filtered.map((item) => { const scenario = demoScenarios.find((demo) => demo.envelope.type === item.type); return <article key={item.id}><div className="capability-icon"><Layers3 /></div><p className="eyebrow">{item.type}</p><h3>{item.displayName}</h3><div className="capability-tags">{item.supports.streaming ? <span>Streaming</span> : null}{item.supports.editing ? <span>Editing</span> : null}{item.supports.fullscreen ? <span>Fullscreen</span> : null}{item.supports.export?.map((format) => <span key={format}>{format}</span>)}</div><dl><div><dt>MIME</dt><dd>{item.supports.mimeTypes?.slice(0, 2).join(", ") || "Protocol data"}</dd></div><div><dt>Extensions</dt><dd>{item.supports.extensions?.map((value) => `.${value}`).slice(0, 4).join(", ") || "—"}</dd></div></dl>{scenario ? <Link href={`/?scenario=${scenario.id}`}>Open live demo →</Link> : <span className="schema-ready"><CheckCircle2 /> Schema + renderer registered</span>}</article>; })}</div></section><section className="gallery-section widget-gallery"><div className="gallery-heading"><div><p className="eyebrow">Controlled rendering</p><h2>Business widgets</h2></div><div className="segmented">{(["ready", "loading", "empty", "error"] as const).map((state) => <button type="button" className={widgetState === state ? "active" : ""} key={state} onClick={() => setWidgetState(state)}>{state}</button>)}</div></div><div className="widget-gallery-grid">{widgets.map((envelope) => <article key={envelope.id}><header><code>{envelope.type}</code><span>compact / full</span></header><RendererHost envelope={envelope} /></article>)}</div></section><section className="gallery-section states-grid"><div><p className="eyebrow">Declarative</p><h2>Catalog only</h2><p>Dynamic forms can compose allow-listed fields, groups and wizard steps. Unknown component names never become executable React.</p><Link className="button primary" href="/?scenario=form">Open form demo</Link></div><div><p className="eyebrow">Open-ended</p><h2>Sandbox required</h2><p>HTML, React and Python have visible isolation, reset and stop controls. Generated code never runs in the host window.</p><Link className="button secondary" href="/?scenario=html">Open sandbox demo</Link></div></section></main>;
}

